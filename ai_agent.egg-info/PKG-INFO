Metadata-Version: 2.4
Name: ai-agent
Version: 0.1.0
Summary: AI code review prototype for catching AI-generated code smells
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: ipykernel>=7.2.0
Requires-Dist: langchain>=0.1.0
Requires-Dist: langchain-community>=0.0.20
Requires-Dist: langchain-google-genai>=1.0.1
Requires-Dist: langchain-groq>=0.0.1
Requires-Dist: langchain-openai>=0.0.6
Requires-Dist: python-dotenv>=1.2.2

AI Agent Review Prototype
=========================

This workspace is a small Python prototype for an AI code review tool.

How the pieces map
------------------

1. main.py is the CLI entrypoint.
2. review_engine.py contains the review rules.
3. review_types.py defines the report shape.

UV workflow
-----------

1. `uv sync` installs the project from `pyproject.toml` and `uv.lock`.
2. `uv run ai-agent --demo` runs the packaged CLI.
3. `uv run ai-agent path/to/file.py` reviews a real file.
4. `uv run ai-agent --diff-file path/to/diff.patch` inspects PR-style diff input.

What the MVP checks
-------------------

- Hallucinated imports or missing dependencies
- Dead code in the current file
- Unnecessary abstraction layers
- Duplicate logic
- Suspicious AI-generated patterns
- Missing tests for the reviewed file

Run it
------

Review a file with `uv`:

```bash
uv run ai-agent path/to/file.py
```

Run the demo sample with `uv`:

```bash
uv run ai-agent --demo
```

Inspect a diff file:

```bash
uv run ai-agent --diff-file path/to/diff.patch
```

Teaching note
-------------

The current architecture is intentionally small:

- input: a Python file path or the built-in demo source
- analysis: AST-based review rules
- output: a score plus actionable findings

That gives us a clean first MVP before adding GitHub PR integration.
